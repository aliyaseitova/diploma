# Формат Excel-файла для KZLEAP

## Как это работает

1. Скачайте шаблон: `GET /api/template` → `KZLEAP_DATA_TEMPLATE.xlsx`
2. Заполните своими данными (см. ниже)
3. Загрузите через Upload Dataset — все расчёты переключатся на ваши данные
4. Все графики и сценарии теперь строятся из загруженного файла

---

## Листы (Sheets)

### 1. `historical_energy` — Исторические энергетические данные
| Колонка | Описание | Единица |
|---------|----------|---------|
| `Year` | Год | число (напр. 2023) |
| `TPES_Mtoe` | Общее первичное потребление энергии | Мтнэ |
| `Electricity_TWh` | Производство/потребление электроэнергии | ТВт·ч |
| `CO2_Mt` | Выбросы CO₂ | Мт CO₂ |

> **Базовый год**: последний год с данными автоматически берётся как базовый для прогнозов.

---

### 2. `historical_demo` — Демографические данные
| Колонка | Описание | Единица |
|---------|----------|---------|
| `Year` | Год | число |
| `Population_M` | Население | млн человек |
| `WorkingAge_pct` | Доля населения 15–64 лет | % |
| `Urban_pct` | Доля городского населения | % |
| `GDP_USD_B` | ВВП | млрд USD |

---

### 3. `elec_mix` — Структура электрогенерации базового года
| Колонка | Описание |
|---------|----------|
| `Source` | Источник: `coal`, `gas`, `hydro`, `wind`, `solar`, `nuclear` |
| `Share_pct` | Доля в % (сумма = 100) |

---

### 4. `scenarios` — Параметры сценариев
Каждая строка — параметр; колонки `BAU`, `MT`, `DD` — значения.

| Параметр | Описание | Пример BAU / MT / DD |
|----------|----------|-----------------------|
| `name` | Название сценария | Business as Usual |
| `description` | Описание | … |
| `gdp_growth` | Темп роста ВВП (доля) | 0.040 / 0.042 / 0.043 |
| `energy_intensity_change` | Изменение энергоёмкости (доля) | -0.010 / -0.020 / -0.030 |
| `coal_share_2050` | Доля угля в электрогенерации к 2050 (доля) | 0.45 / 0.25 / 0.05 |
| `renewables_2030` | Доля ВИЭ к 2030 (доля) | 0.08 / 0.15 / 0.22 |
| `renewables_2050` | Доля ВИЭ к 2050 (доля) | 0.15 / 0.40 / 0.70 |
| `nuclear_gw_2035` | Мощность АЭС к 2035 (ГВт) | 0.0 / 1.2 / 2.4 |
| `co2_price_2030` | Цена CO₂ к 2030 (USD/т) | 5 / 20 / 50 |
| `co2_price_2050` | Цена CO₂ к 2050 (USD/т) | 10 / 50 / 150 |
| `ev_share_2050` | Доля EV в транспорте к 2050 (доля) | 0.10 / 0.30 / 0.80 |

---

### 5. `ndc_targets` — Цели NDC и нейтральности
| Параметр | Описание | Пример |
|----------|----------|--------|
| `base_year` | Базовый год NDC | 1990 |
| `base_co2_mt` | Выбросы в базовом году (Мт) | 290.0 |
| `ndc_unconditional_pct` | Безусловное сокращение к 2030 (%) | -15.0 |
| `ndc_conditional_pct` | Условное сокращение к 2030 (%) | -25.0 |
| `neutrality_year` | Год углеродной нейтральности | 2060 |

---

## API

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/api/template` | Скачать шаблон XLSX |
| `POST` | `/api/upload` | Загрузить Excel (.xlsx) или CSV |
| `GET` | `/api/active-dataset` | Проверить активный датасет |
| `GET` | `/api/historical` | Исторические данные (из Excel если загружен) |
| `POST` | `/api/run` | Запустить сценарий (данные из Excel) |

---

## Примечания

- **Не удаляйте листы** — все 5 листов обязательны
- **Не переименовывайте листы** — названия листов фиксированы
- **Заголовки** в первой строке каждого листа (не изменять)
- Значения долей (gdp_growth, renewables_2030 и т.д.) указываются **десятичной дробью** (0.15 = 15%)
- Значения `Share_pct` в `elec_mix` указываются **в процентах** (61.0 = 61%)
